<div class="new-merchant-grid">
  @foreach (var activeGroup in ActiveData.MerchantGroups.Where(m => m.IsActive))
  {
    <Merchant ClassName="new-merchant-grid__item" MerchantGroup="@activeGroup" Server="@Server" />
  }
</div>


@code {
    [CascadingParameter]
    public ActiveDataController ActiveData { get; init; } = default!;

    [Parameter, EditorRequired]
    public string Server { get; set; } = default!;
}